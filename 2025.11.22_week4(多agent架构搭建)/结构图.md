```mermaid
flowchart TD
  %% 顶层流程
  START([开始]) --> PREP[准备上下文<br/>prepareContext]
  PREP --> ROUTER{意图路由<br/>routerAgent}

  %% Router 三路分支
  ROUTER -->|home_control| HOME[家居控制 Agent<br/>homePlanningAgent]
  ROUTER -->|vision_analysis| VISION[视觉分析 Agent<br/>visionPlanningAgent]
  ROUTER -->|general_chat| GEN_RESP[生成最终回答<br/>generateResponse]

  %% 家居 Agent 分支
  HOME --> HOME_ROUTE{是否有家居工具调用？<br/>routeFromHomePlanning}
  HOME_ROUTE -->|否| GEN_RESP

  HOME_ROUTE -->|是| LIGHT[控制灯光<br/>controlLight]
  HOME_ROUTE -->|是| CURTAIN[控制窗帘<br/>controlCurtain]
  HOME_ROUTE -->|是| AIRCON[控制空调<br/>controlAircon]
  HOME_ROUTE -->|是| SWITCH[通用开关<br/>controlUniSwitch]
  HOME_ROUTE -->|是| FLOOR[地暖控制<br/>controlFloorHeating]
  HOME_ROUTE -->|是| SCENE[场景触发<br/>controlScene]
  HOME_ROUTE -->|是| BUILD_SCENE[场景编排<br/>buildScene]
  HOME_ROUTE -->|是| MUSIC_PLAY[音乐播放<br/>musicPlay]
  HOME_ROUTE -->|是| MUSIC_CTRL[音乐控制<br/>musicControl]

  %% 视觉 Agent 分支
  VISION --> VISION_ROUTE{是否有视觉工具调用？<br/>routeFromVisionPlanning}
  VISION_ROUTE -->|否| GEN_RESP

  VISION_ROUTE -->|get_camera_snapshot| SNAPSHOT[获取摄像头快照<br/>cameraSnapshot]

  %% 工具统一汇聚
  LIGHT --> AGG[工具结果聚合<br/>aggregateResults]
  CURTAIN --> AGG
  AIRCON --> AGG
  SWITCH --> AGG
  FLOOR --> AGG
  SCENE --> AGG
  BUILD_SCENE --> AGG
  MUSIC_PLAY --> AGG
  MUSIC_CTRL --> AGG
  SNAPSHOT --> AGG

  %% 聚合后智能应答与最终回复
  AGG --> INTEL_RESP[基于工具结果生成智能回复<br/>generateIntelligentResponse]
  INTEL_RESP --> GEN_RESP
  GEN_RESP --> END([结束])